channels:
  - conda-forge
  - nodefaults
dependencies:
  - python
  - pip
  - pip:
    # analysis
    - numpy
    - pandas[parquet]
    # development
    - dvc[gs]==3.56.0
    - ipykernel
    - black
    - nbconvert
    - notebook<7  # newest version is incompatible with nbconvert
    - jupyter_contrib_nbextensions
    - jupytext
